---
phase: 02-service-layer-commercial-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/solana-live-adapter.ts
  - tests/mycelium-hypha.ts
  - tests/mycelium-rhizome.ts
autonomous: true
requirements: [LIC-01, LIC-02, LIC-03, LIC-04, LIC-05, ROY-01, ROY-02, ROY-03, ROY-04]
user_setup:
  - service: devnet-usdc
    why: "Testing USDC license payments and royalty distribution"
    env_vars:
      - name: USDC_MINT
        source: "Use devnet USDC 4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU or mint custom SPL token for testing"

must_haves:
  truths:
    - "IP owner can create a license template with type, price in USDC, royalty rate, territory, and exclusivity"
    - "Licensee can acquire a license by paying USDC to the licensor"
    - "License verification returns licensed/not-licensed with template details"
    - "Royalty splits distribute USDC to all recipients atomically with platform fee deducted"
    - "Four license archetypes (Creative Commons, Commercial, Exclusive, AI Training) are all functional"
    - "Rhizome test suite covers config, deposit, distribution, edge cases, and security constraints"
  artifacts:
    - path: "src/solana-live-adapter.ts"
      provides: "Live Hypha + Rhizome adapter methods with USDC"
      contains: "createLicense"
    - path: "tests/mycelium-hypha.ts"
      provides: "Hypha program test suite"
      min_lines: 100
    - path: "tests/mycelium-rhizome.ts"
      provides: "Rhizome program test suite (extended for USDC)"
      min_lines: 100
  key_links:
    - from: "src/solana-live-adapter.ts"
      to: "src/idl/mycelium_hypha.json"
      via: "Anchor Program instantiation"
      pattern: "mycelium_hypha"
    - from: "src/solana-live-adapter.ts"
      to: "src/idl/mycelium_rhizome.json"
      via: "Anchor Program instantiation"
      pattern: "mycelium_rhizome"
    - from: "src/solana-live-adapter.ts"
      to: "@solana/spl-token"
      via: "USDC transfer in acquireLicense and distributeRoyalties"
      pattern: "getOrCreateAssociatedTokenAccount|createTransferCheckedInstruction"
    - from: "src/solana-live-adapter.ts"
      to: "src/services/indexer/queries.ts"
      via: "PostgreSQL queries for listLicenses and verifyLicense fallback"
      pattern: "getLicensesByIP|searchIPAssets"
---

<objective>
Implement live Hypha licensing (create, acquire, verify) and Rhizome royalty distribution with USDC SPL token payments. Replace all TODO/throw stubs in solana-live-adapter.ts with real on-chain program calls via Anchor IDL client.

Purpose: This is the commercial engine -- IP owners need to monetize through licensing, and royalty recipients need to receive USDC payments. Without this, Mycelium is registration-only with no revenue path.

Output: Live adapter methods for all Hypha and Rhizome operations, extended test suites for both programs.
</objective>

<execution_context>
@.planning/phases/02-service-layer-commercial-engine/02-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-secure-foundation/01-01-SUMMARY.md
@.planning/phases/01-secure-foundation/01-02-SUMMARY.md
@.planning/phases/01-secure-foundation/01-03-SUMMARY.md
@.planning/phases/02-service-layer-commercial-engine/02-01-SUMMARY.md

<interfaces>
<!-- Existing adapter interface methods that need live implementations -->

From src/solana-adapter.ts:
```typescript
// Licensing (mycelium-hypha program) — currently throw "not implemented" in live adapter
createLicense(params: CreateLicenseParams): Promise<LicenseTemplate>;
acquireLicense(params: AcquireLicenseParams): Promise<LicenseToken>;
verifyLicense(ipAsset: string, wallet: string): Promise<LicenseVerification>;
listLicenses(ipAsset: string): Promise<LicenseTemplate[]>;

export interface CreateLicenseParams {
  agentId: string;
  ipAsset: string;
  licenseType: LicenseType;
  commercialUse: boolean;
  derivativesAllowed: DerivativePolicy;
  aiTraining: AITrainingPolicy;
  priceUsdc: number;      // in USDC lamports (6 decimals)
  royaltyBps: number;     // basis points
  maxDerivativeDepth: number;
  territories: string[];
  exclusive: boolean;
  expiryTimestamp?: number;
  maxLicenses?: number;
}

export interface AcquireLicenseParams {
  agentId: string;
  licenseTemplate: string;  // pubkey of template
}
```

PDA Seeds (from ARCHITECTURE.md):
```
Hypha:  ["license_template", ip_asset, licensor]
        ["license", template, licensee]
Rhizome: ["royalty_config", ip_asset]
         ["royalty_vault", ip_asset]
```

IDL files available at:
- src/idl/mycelium_hypha.json
- src/idl/mycelium_rhizome.json

USDC Config:
- Devnet mint: 4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU
- Mainnet mint: EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
- Decimals: 6 (1_000_000 = $1.00)
- Must create ATAs before transfer: getOrCreateAssociatedTokenAccount
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Hypha live adapter (createLicense, acquireLicense, verifyLicense) with USDC</name>
  <files>
    src/solana-live-adapter.ts
    tests/mycelium-hypha.ts
  </files>
  <read_first>
    src/solana-live-adapter.ts
    src/solana-adapter.ts
    src/types.ts
    src/idl/mycelium_hypha.json
    src/idl/mycelium_spore.json
    tests/mycelium-hypha.ts
    .planning/phases/02-service-layer-commercial-engine/02-RESEARCH.md
  </read_first>
  <action>
    **Step 1: Add Hypha Program to SolanaLiveAdapter constructor:**
    - Load mycelium_hypha.json IDL (same pattern as Spore: readFileSync + JSON.parse)
    - Create `this.hyphaProgram = new Program(hyphaIdl, this.provider)` alongside existing sporeProgram
    - Add USDC_MINT constant: `new PublicKey(process.env.USDC_MINT || "4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU")`
    - Add USDC_DECIMALS = 6 constant

    **Step 2: Implement createLicense method** (replace the "not implemented" throw):
    - Get agent keypair via getOrCreateWallet (use BIP-44 from key-vault if available, fallback to existing agentWallets Map)
    - Derive license_template PDA: `["license_template", ipAssetPubkey.toBuffer(), licensor.publicKey.toBuffer()]` using HYPHA program ID
    - Map CreateLicenseParams to Hypha program instruction params:
      - licenseType: map string to enum index (CreativeCommons=0, Commercial=1, Exclusive=2, AITraining=3)
      - priceUsdc: pass as BN (BigInt-safe) -- already in USDC lamports (6 decimals)
      - royaltyBps: pass as u16
      - territories: convert string[] to byte array or hash
      - exclusive: boolean
      - expiryTimestamp: pass as i64 or null
    - Call `this.hyphaProgram.methods.createLicenseTemplate(params).accountsPartial({...}).signers([licensor]).rpc()`
    - Fetch created template: `this.hyphaProgram.account.licenseTemplate.fetch(templatePda)`
    - Map to LicenseTemplate type and return

    **Step 3: Implement acquireLicense method** (replace the "not implemented" throw):
    - Get agent keypair (licensee)
    - Fetch license template to get IP asset pubkey and price
    - Derive license PDA: `["license", templatePubkey.toBuffer(), licensee.publicKey.toBuffer()]` using HYPHA program ID
    - USDC payment flow (CRITICAL -- 6 decimals, not 9):
      1. `getOrCreateAssociatedTokenAccount(connection, payer, USDC_MINT, licensee.publicKey)` -- licensee ATA
      2. `getOrCreateAssociatedTokenAccount(connection, payer, USDC_MINT, licensorPubkey)` -- licensor ATA
      3. Create `createTransferCheckedInstruction(licenseeAta, USDC_MINT, licensorAta, licensee.publicKey, priceUsdcLamports, 6)` 
      4. Build transaction: USDC transfer + Hypha acquireLicense instruction
      5. Send as single atomic transaction
    - Fetch created license: `this.hyphaProgram.account.license.fetch(licensePda)`
    - Map to LicenseToken type and return

    **Step 4: Implement verifyLicense method:**
    - Given ipAsset pubkey and wallet pubkey
    - Query all license templates for this IP asset (fetch from indexer queries if available, fallback to on-chain getProgramAccounts with memcmp filter on ip_asset field)
    - For each template, derive license PDA with the wallet as licensee
    - Try to fetch each license PDA -- if account exists, wallet is licensed
    - Return LicenseVerification: { licensed: boolean, licenseToken: found license or null, availableLicenses: all templates }

    **Step 5: Implement listLicenses method:**
    - Query indexer (src/services/indexer/queries.ts getLicensesByIP) if available
    - Fallback: use hyphaProgram.account.licenseTemplate.all() with memcmp filter on ip_asset bytes
    - Map to LicenseTemplate[] and return

    **Step 6: Update existing getProvenance method:**
    - Replace the `licenses: []` TODO with actual license query (via indexer or on-chain)
    - Replace the `disputes: []` TODO with placeholder (DRP is Phase 3)

    **Step 7: Extend tests/mycelium-hypha.ts** with comprehensive test cases:
    - Create license template (all 4 archetypes: CreativeCommons, Commercial, Exclusive, AITraining) -- LIC-01, LIC-02
    - Acquire license with USDC payment -- LIC-03
    - Verify licensed wallet returns true -- LIC-04
    - Verify unlicensed wallet returns false -- LIC-04
    - Reject license creation for non-existent IP asset
    - Reject acquisition with insufficient USDC balance
    - Verify exclusive license blocks second acquisition
  </action>
  <verify>
    <automated>grep -c "createLicense\|acquireLicense\|verifyLicense\|listLicenses" src/solana-live-adapter.ts && grep "throw.*not implemented" src/solana-live-adapter.ts; echo "Exit: $? (1 = no more 'not implemented' throws for Hypha methods)"</automated>
  </verify>
  <acceptance_criteria>
    - grep "hyphaProgram" src/solana-live-adapter.ts returns match
    - grep "mycelium_hypha" src/solana-live-adapter.ts returns match (IDL import)
    - grep "getOrCreateAssociatedTokenAccount" src/solana-live-adapter.ts returns match
    - grep "createTransferCheckedInstruction" src/solana-live-adapter.ts returns match
    - grep "USDC_MINT\|usdc_mint\|usdcMint" src/solana-live-adapter.ts returns match
    - grep "license_template.*ip_asset\|licenseTemplate" src/solana-live-adapter.ts returns match (PDA derivation)
    - grep "not implemented" src/solana-live-adapter.ts does NOT match for createLicense or acquireLicense (both replaced)
    - grep "it(" tests/mycelium-hypha.ts returns >= 7 test cases
    - grep "CreativeCommons\|Commercial\|Exclusive\|AITraining" tests/mycelium-hypha.ts returns matches (4 archetypes)
    - grep "USDC\|usdc\|spl-token" tests/mycelium-hypha.ts returns match (USDC payment test)
  </acceptance_criteria>
  <done>
    createLicense, acquireLicense, verifyLicense, and listLicenses all work against devnet via Anchor IDL client. USDC payments use getOrCreateAssociatedTokenAccount + createTransferCheckedInstruction with 6 decimals. Test suite covers all 4 license archetypes, USDC payment, verification, and error cases. No "not implemented" throws remain for Hypha methods.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Rhizome USDC royalty distribution live adapter and extend test suite</name>
  <files>
    src/solana-live-adapter.ts
    tests/mycelium-rhizome.ts
  </files>
  <read_first>
    src/solana-live-adapter.ts
    src/solana-adapter.ts
    src/types.ts
    src/idl/mycelium_rhizome.json
    tests/mycelium-rhizome.ts
    .planning/phases/01-secure-foundation/01-03-SUMMARY.md
  </read_first>
  <action>
    **IMPORTANT: Verify tests/mycelium-rhizome.ts exists before proceeding.** Phase 1 plan 01-03 created this file with 9 SOL-based test cases. If the file exists, extend it. If it does NOT exist (unexpected), create it from scratch with both SOL and USDC test cases -- see fallback instructions at end of this action.

    **Step 1: Add Rhizome Program to SolanaLiveAdapter constructor:**
    - Load mycelium_rhizome.json IDL (same pattern as Spore/Hypha)
    - Create `this.rhizomeProgram = new Program(rhizomeIdl, this.provider)` alongside sporeProgram and hyphaProgram

    **Step 2: Add Rhizome-related methods to SolanaLiveAdapter:**
    Note: The SolanaAdapter interface currently exposes Rhizome operations through the wallet balance method and the royalty fields on IPAsset. Add internal methods used by the MCP tools:

    - Add `configureRoyalty(ipAssetPubkey: PublicKey, creator: Keypair, recipients: Array<{address: PublicKey, basisPoints: number}>, platformFeeBps: number): Promise<string>`:
      1. Derive royalty_config PDA: `["royalty_config", ipAssetPubkey.toBuffer()]` using RHIZOME program ID
      2. Derive royalty_vault PDA: `["royalty_vault", ipAssetPubkey.toBuffer()]` using RHIZOME program ID
      3. Recipients must have basisPoints summing to 10_000
      4. platformFeeBps must be <= 1000 (10%)
      5. Call `this.rhizomeProgram.methods.configureRoyalty({recipients, platformFeeBps, platformWallet}).accountsPartial({...}).signers([creator]).rpc()`
      6. Return tx signature

    - Add `depositRoyalty(ipAssetPubkey: PublicKey, depositor: Keypair, amountUsdcLamports: bigint): Promise<string>`:
      1. Derive royalty_vault PDA
      2. USDC transfer from depositor ATA to vault ATA:
         - `getOrCreateAssociatedTokenAccount` for depositor and vault
         - `createTransferCheckedInstruction` with 6 decimals
      3. Call Rhizome deposit_royalty instruction in same transaction
      4. Return tx signature

    - Add `distributeRoyalties(ipAssetPubkey: PublicKey, caller: Keypair): Promise<string>`:
      1. Fetch RoyaltyConfig to get recipients list and platform_wallet
      2. Derive vault PDA, get vault USDC ATA
      3. Build instruction accounts: vault ATA + all recipient ATAs + platform wallet ATA
      4. Ensure all recipient ATAs exist (getOrCreateAssociatedTokenAccount for each)
      5. Call Rhizome distribute_royalties instruction -- single atomic transaction splits vault to all recipients minus platform fee
      6. Return tx signature

    **Step 3: Update getWalletBalance to return real USDC balance:**
    - Replace the `// TODO: Query actual USDC SPL token balance` comment on line ~571
    - Use `getOrCreateAssociatedTokenAccount(connection, payer, USDC_MINT, walletPubkey)` to get ATA
    - Use `getAccount(connection, ata.address)` to get token balance
    - Return `Number(tokenAccount.amount) / 1_000_000` as usdcBalance (convert USDC lamports to dollars)

    **Step 4: Extend tests/mycelium-rhizome.ts** -- The file should have 9 SOL-based test cases from Phase 1. Add 7 USDC-specific tests:
    - USDC deposit to royalty vault -- ROY-02
    - USDC distribution to 3 recipients with correct splits -- ROY-02, ROY-03
    - Platform fee correctly deducted in USDC -- ROY-03
    - Maximum 8 recipients with USDC -- ROY-01
    - Zero balance distribution rejected -- edge case
    - Verify all recipients received correct proportional USDC amounts -- ROY-03
    - Full end-to-end: configure -> deposit USDC -> distribute -> verify balances -- ROY-04

    Important: All USDC amounts use integer lamports (6 decimals). Never use floating point.
    Example: $10 USDC = 10_000_000 lamports. $0.50 = 500_000 lamports.

    **FALLBACK (if tests/mycelium-rhizome.ts does not exist):**
    Create the file from scratch with the standard Anchor test boilerplate (import anchor, describe block, provider setup). Include ALL 16 test cases:
    - 9 SOL-based tests: configure royalty (happy path), configure with max 8 recipients, configure with invalid bps sum (reject), deposit SOL, distribute SOL to 3 recipients, distribute with platform fee, non-creator cannot configure (reject), non-depositor cannot distribute (reject), double-configure rejected
    - 7 USDC-specific tests as listed in Step 4 above
  </action>
  <verify>
    <automated>grep -c "rhizomeProgram\|configureRoyalty\|depositRoyalty\|distributeRoyalties" src/solana-live-adapter.ts && grep -c "it(" tests/mycelium-rhizome.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "rhizomeProgram" src/solana-live-adapter.ts returns match
    - grep "mycelium_rhizome" src/solana-live-adapter.ts returns match (IDL import)
    - grep "configureRoyalty" src/solana-live-adapter.ts returns match
    - grep "depositRoyalty" src/solana-live-adapter.ts returns match
    - grep "distributeRoyalties" src/solana-live-adapter.ts returns match
    - grep "royalty_config\|royaltyConfig" src/solana-live-adapter.ts returns match (PDA derivation)
    - grep "royalty_vault\|royaltyVault" src/solana-live-adapter.ts returns match (PDA derivation)
    - grep "TODO.*USDC" src/solana-live-adapter.ts does NOT return match (TODO replaced)
    - grep "it(" tests/mycelium-rhizome.ts returns >= 16 (9 existing SOL + 7 new USDC tests)
    - grep "USDC\|usdc\|spl.token\|getOrCreateAssociatedTokenAccount" tests/mycelium-rhizome.ts returns match
    - grep "1_000_000\|lamports\|USDC_DECIMALS" src/solana-live-adapter.ts returns match (integer arithmetic, no floats)
  </acceptance_criteria>
  <done>
    Rhizome live adapter supports configureRoyalty, depositRoyalty, distributeRoyalties with USDC SPL token. getWalletBalance returns real USDC balance. Test suite has 16+ cases covering SOL and USDC flows, platform fee deduction, max recipients, and end-to-end distribution. All USDC arithmetic uses integer lamports (6 decimals).
  </done>
</task>

</tasks>

<verification>
1. No "not implemented" throws remain in solana-live-adapter.ts for Hypha or Rhizome methods
2. All USDC amounts handled as integer lamports (6 decimals) -- no floating point
3. All ATAs created before transfer (getOrCreateAssociatedTokenAccount)
4. Hypha test suite covers all 4 license archetypes
5. Rhizome test suite covers USDC deposit, distribution, and platform fee
6. PDA derivation matches seeds defined in ARCHITECTURE.md
</verification>

<success_criteria>
- createLicense creates template on-chain via Hypha IDL client
- acquireLicense pays USDC and creates license PDA
- verifyLicense returns correct licensed/not-licensed status
- configureRoyalty creates RoyaltyConfig with up to 8 recipients
- distributeRoyalties splits USDC vault atomically to all recipients
- getWalletBalance returns real USDC balance (not SOL proxy)
- 23+ total test cases across Hypha and Rhizome test files (7 Hypha + 16 Rhizome)
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-layer-commercial-engine/02-02-SUMMARY.md`
</output>
