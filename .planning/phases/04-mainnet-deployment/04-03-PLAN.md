---
phase: 04-mainnet-deployment
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - docs/multisig-setup.md
  - docs/authority-transfer.sh
autonomous: false
requirements: [SEC-06]

must_haves:
  truths:
    - "Squads v4 multisig creation procedure is documented with exact steps"
    - "Authority transfer script transfers upgrade authority for all 5 programs to Squads vault PDA"
    - "2-of-3 minimum threshold is enforced in multisig configuration"
    - "Test transaction procedure is documented to verify multisig works before authority transfer"
  artifacts:
    - path: "docs/multisig-setup.md"
      provides: "Complete Squads v4 multisig creation and verification guide"
      min_lines: 60
    - path: "docs/authority-transfer.sh"
      provides: "Executable script for transferring upgrade authority to Squads vault"
  key_links:
    - from: "docs/authority-transfer.sh"
      to: "Anchor.toml"
      via: "Reads mainnet program IDs for authority transfer"
      pattern: "set-upgrade-authority"
    - from: "docs/multisig-setup.md"
      to: "docs/deployment-runbook.md"
      via: "Multisig must be created before deployment (prerequisite)"
      pattern: "Squads"
---

<objective>
Document the Squads v4 multisig setup procedure and create the authority transfer script that makes all 5 programs governance-controlled.

Purpose: SEC-06 requires that no single key can modify the protocol on mainnet. The Squads multisig (2-of-3) becomes the upgrade authority. This plan includes a checkpoint because the multisig member identities are a user decision (research flagged this as TBD).

Output: Multisig setup guide, executable authority transfer script, verified multisig governance for all programs.
</objective>

<execution_context>
@.planning/phases/04-mainnet-deployment/04-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-mainnet-deployment/04-01-SUMMARY.md
@Anchor.toml

<interfaces>
<!-- From 04-RESEARCH.md: Squads architecture -->

Squads v4 multisig structure:
- Program: SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf
- Member permissions: Proposer (1), Voter (2), Executor (4) -- bitmask combinable (7 = all)
- Vault PDA: derived from multisig address, index 0
- Threshold: 2-of-3 minimum

Authority transfer:
```bash
solana program set-upgrade-authority <PROGRAM_ID> \
  --new-upgrade-authority <SQUADS_VAULT_PDA> \
  --keypair ~/solana-keys/id.json \
  -u mainnet-beta
```

Verification:
```bash
solana program show <PROGRAM_ID> -u mainnet-beta | grep "Authority"
# Should show Squads vault PDA, not deployer key
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide multisig members</name>
  <decision>Who are the 3 multisig members for Mycelium Protocol governance?</decision>
  <context>
    Squads v4 multisig with 2-of-3 threshold controls all program upgrades on mainnet. This is the single most important security decision for the protocol. Members need Solana wallets (hardware wallets strongly recommended).

    Research recommended: Aji Pratomo as Member 1 (Proposer + Voter + Executor = permission 7). Members 2 and 3 need at minimum Voter permission (2).

    The multisig address and vault PDA will be derived from the member configuration -- they cannot be changed later without all members agreeing to migrate.
  </context>
  <options>
    <option id="option-a">
      <name>Aji + 2 trusted team members</name>
      <pros>Operational flexibility, team can approve upgrades when Aji is unavailable</pros>
      <cons>More wallets to manage, team members need Solana wallet setup</cons>
    </option>
    <option id="option-b">
      <name>Aji + 2 hardware wallets (same person, different devices)</name>
      <pros>Simpler key management, single point of trust, faster approvals</pros>
      <cons>Single person is bus factor, 2-of-3 is effectively 1-of-1 with extra steps</cons>
    </option>
    <option id="option-c">
      <name>Aji + 1 team member + 1 cold storage backup</name>
      <pros>Balance of operational speed and security, cold storage as emergency recovery</pros>
      <cons>Cold storage wallet harder to use for routine upgrades</cons>
    </option>
  </options>
  <resume-signal>Provide: (1) option choice, (2) wallet addresses for each member (or "will provide later")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create multisig setup guide and authority transfer script</name>
  <files>
    docs/multisig-setup.md,
    docs/authority-transfer.sh
  </files>
  <read_first>
    Anchor.toml,
    .planning/phases/04-mainnet-deployment/04-RESEARCH.md,
    .planning/phases/04-mainnet-deployment/04-01-SUMMARY.md
  </read_first>
  <action>
    Use the multisig member decision from Task 1 checkpoint. If "will provide later" was selected for wallet addresses, use placeholder addresses with clear `<MEMBER_N_WALLET>` markers.

    **1. Create `docs/multisig-setup.md`:**

    Section 1: Prerequisites
    - Each member needs a Solana wallet (Phantom, Solflare, or hardware wallet recommended)
    - Member 1 (Aji): needs SOL for multisig creation fee (~0.02 SOL)
    - All members need to share their public key (wallet address)

    Section 2: Create Squads Multisig (step-by-step with screenshots)
    a. Go to app.squads.so
    b. Connect Member 1's wallet
    c. Click "Create Multisig"
    d. Add 3 members with permissions:
       - Member 1 (Aji): Permission 7 (Proposer + Voter + Executor)
       - Member 2: Permission 2 (Voter) -- or 6 (Voter + Executor) if trusted
       - Member 3: Permission 2 (Voter)
    e. Set threshold: 2
    f. Confirm and sign transaction
    g. Record multisig address and vault PDA (shown in Squads UI)

    Section 3: Verify Multisig Works (CRITICAL -- do before authority transfer)
    a. Create a test transaction (e.g., send 0.001 SOL from vault to Member 1)
    b. Member 1 proposes
    c. Member 2 approves
    d. Verify transaction executes
    e. If test fails, DO NOT proceed to authority transfer

    Section 4: Record Addresses
    - Multisig address: ____________
    - Vault PDA (index 0): ____________
    - These go into GitHub secrets: MAINNET_MULTISIG and MAINNET_MULTISIG_VAULT

    **2. Create `docs/authority-transfer.sh`:**

    Executable bash script that:
    a. Takes SQUADS_VAULT_PDA as first argument
    b. Reads all 5 mainnet program IDs from Anchor.toml [programs.mainnet-beta] section
    c. For each program: runs `solana program set-upgrade-authority <ID> --new-upgrade-authority $SQUADS_VAULT_PDA --keypair ~/solana-keys/id.json -u mainnet-beta`
    d. After each transfer: runs `solana program show <ID> -u mainnet-beta` and grep for the new authority to confirm
    e. Includes a `--dry-run` flag that prints commands without executing
    f. Includes confirmation prompt before each transfer ("Are you sure? This is irreversible. [y/N]")
    g. Logs all operations to `authority-transfer.log` with timestamps

    The script header should include:
    ```bash
    #!/bin/bash
    # Mycelium Protocol -- Authority Transfer to Squads Multisig
    # DANGER: This transfers program upgrade authority. Irreversible without multisig approval.
    # Run with --dry-run first to verify commands.
    # Prerequisites: Squads multisig created and tested (docs/multisig-setup.md)
    ```
  </action>
  <verify>
    <automated>
      test -f docs/multisig-setup.md && test -f docs/authority-transfer.sh && grep -c "set-upgrade-authority" docs/authority-transfer.sh && grep -c "dry-run" docs/authority-transfer.sh && wc -l docs/multisig-setup.md | awk '{if ($1 >= 60) print "PASS"; else print "FAIL"}'
    </automated>
  </verify>
  <acceptance_criteria>
    - docs/multisig-setup.md exists with >= 60 lines
    - Contains step-by-step Squads creation procedure
    - Contains test transaction verification section
    - docs/authority-transfer.sh is executable with --dry-run flag
    - Script transfers authority for all 5 programs
    - Script includes confirmation prompts and logging
    - Script reads program IDs from Anchor.toml, not hardcoded
  </acceptance_criteria>
  <done>Squads multisig setup fully documented with test procedure, authority transfer script handles all 5 programs with dry-run mode and confirmation prompts</done>
</task>

</tasks>

<verification>
1. `docs/multisig-setup.md` has step-by-step Squads creation with test transaction verification
2. `docs/authority-transfer.sh` with `--dry-run` prints 5 `solana program set-upgrade-authority` commands
3. Script reads mainnet program IDs from Anchor.toml (not hardcoded)
4. All docs reference 2-of-3 threshold as minimum
</verification>

<success_criteria>
- Multisig member decision recorded
- Setup guide enables anyone to create the Squads multisig following documented steps
- Authority transfer script is safe (dry-run, confirmations, logging) and covers all 5 programs
- Post-transfer verification confirms Squads vault PDA is the authority for each program
</success_criteria>

<output>
After completion, create `.planning/phases/04-mainnet-deployment/04-03-SUMMARY.md`
</output>
